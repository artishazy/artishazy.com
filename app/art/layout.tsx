import type {Metadata} from "next";
export const metadata:Metadata={title:"Цифровой арт",description:"Цифровое искусство, ASCII, генеративная графика и исследования формы.",alternates:{canonical:"/art"}};
export default function Layout({children}:{children:React.ReactNode}){return children}
